defmodule <%= app_module %>.WWW do
  use Raxx.BluePrint, Path.join(__DIR__, "www.apib")
  use Raxx.Static, Path.join(__DIR__, "public")

  def start_link(config, options) do
    Ace.HTTP2.Service.start_link({__MODULE__, config}, options)
  end
end
