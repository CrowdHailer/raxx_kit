<%= if @docker do %>version: '2'

services:
  <%= @name %>:
    build:
      context: "."
      dockerfile: "Dockerfile"
    ports:
      - 8080:8080
      - 8443:8443
    volumes:
      - .:/opt/app
      - ./deps_docker:/opt/app/deps
      - ./_build_docker:/opt/app/_build
<% end %>
