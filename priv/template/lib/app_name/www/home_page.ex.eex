defmodule <%= @module %>.WWW.HomePage do
  use Raxx.Server
  use <%= @module %>.WWW.Layout, arguments: []

  @impl Raxx.Server
  def handle_request(_request, _state) do
    response(:ok)
    |> render()
  end
end
